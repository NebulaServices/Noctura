---
import Layout from '@layout';
import "@css/style.css";
import "@css/console.css";
import Obfuscated from '@components/Obfuscated.astro';

const page = Astro.url.pathname.replace(/\/$/g, '').split('/').pop();

const tabs = [
    ["Scripts", "/console/scripts"],
    ["Themes", "/console/themes"],
];

const { title } = Astro.props;
---

<Layout title="Console">
    <div class="console">
        <div class="console-left">
            {
                tabs.map(tab => (
                    <a data-cold class={"console-navlink" + (tab[1].split('/').pop() == page ? " console-navlink-active" : "")} href={tab[1]}>{tab[0]}</a>
                ))
            }
        </div>
        
        <div class="console-right">
            <div class="console-page">
                <h1 class="text-3xl">
                    <Obfuscated word={title} />
                </h1>
                
                <slot />
            </div>
        </div>
    </div>
</Layout>