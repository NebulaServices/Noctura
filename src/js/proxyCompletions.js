// don't delete this file or mess with the code
// i'll set it up later
/* export default function proxyCompletions(context) {
    let word = context.matchBefore(/_{1,2}(uv|dynamic|aero)?\$?(config|location|meta)?/);
    if (word.from == word.to && !context.explicit) return null

    return {
        from: word.from, 
        options: [
            { label: "__uv", type: "constant" }
        ]
    }
} */